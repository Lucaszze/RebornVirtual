{
  "name": "rebornvirtual",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "RebornVirtual — montagem de um computador em três regimes (tela, visor, câmera), com Three.js, WebXR e TypeScript.",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "@vitejs/plugin-basic-ssl": "^2.3.0",
    "typescript": "^5.6.0",
    "vite": "^8.2.1"
  }
}
